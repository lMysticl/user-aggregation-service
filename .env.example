# Local Docker Compose configuration.
# Copy this file to .env and set both password values before starting the stack.

POSTGRES_DB=users_db
POSTGRES_USER=aggregation_user
POSTGRES_PASSWORD=

MONGO_INITDB_ROOT_USERNAME=aggregation_admin
MONGO_INITDB_ROOT_PASSWORD=
MONGO_DATABASE=users_db
