spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  data:
    mongodb:
      uri: mongodb://localhost:27017/aggregation_service_test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  cache:
    type: caffeine

aggregation:
  query-timeout: 500ms
  demo-data:
    enabled: false
